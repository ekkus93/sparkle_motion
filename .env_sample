# sparkle_motion Colab/.env sample
# Copy this file to `.env` (kept out of git) and replace the placeholder values
# with real credentials before running the notebook or FunctionTools.

# --- Required ADK + auth settings ---
ADK_PROJECT=sparkle-motion                       # Your ADK project slug
ADK_API_KEY=sk-live-REPLACE_ME                   # Issue via ADK console or `adk keys create`
GOOGLE_APPLICATION_CREDENTIALS=/abs/path/to/service-account.json
ADK_USE_FIXTURE=1                                # Set to 0 only when targeting the real control plane
ADK_PUBLISH_INTEGRATION=0                        # Enable (set to 1) only when running publish tests

# --- Workflow agent endpoints (defaults match local-colab profile) ---
PRODUCTION_AGENT_BASE=http://127.0.0.1:8200
SCRIPT_AGENT_BASE=http://127.0.0.1:8101

# --- Artifact storage overrides ---
ADK_ARTIFACTS_GCS_BUCKET=                        # Optional: bucket for ADK artifact uploads
FINAL_VIDEO_DIR=/content/SparkleMotion/final_videos

# --- GPU / smoke toggles ---
SMOKE_IMAGES=0                                   # Set to 1 to exercise the real images_sdxl adapter
IMAGES_SDXL_FIXTURE_ONLY=1                       # Set to 0 when SMOKE_IMAGES=1 and GPU is available
IMAGES_SDXL_MODEL=stabilityai/stable-diffusion-xl-base-1.0

SMOKE_VIDEOS=0                                   # Set to 1 to exercise the real videos_wan adapter
VIDEOS_WAN_FIXTURE_ONLY=1                        # Set to 0 when SMOKE_VIDEOS=1 and GPU is available
VIDEOS_WAN_MODEL=Wan-AI/Wan2.1-FLF2V-14B-720P-diffusers

SMOKE_TTS=0                                      # Set to 1 to run real TTS adapter smoke tests
SMOKE_LIPSYNC=0                                  # Enable to drive wav2lip smoke paths
SMOKE_ASSEMBLE=0                                 # Enable to run assemble_ffmpeg with FFmpeg installed

# --- Misc notebook helpers ---
WORKSPACE_NAME=SparkleMotion                     # Folder name under Google Drive when mounted
MOUNT_POINT=/content/drive
