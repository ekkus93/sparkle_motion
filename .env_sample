# sparkle_motion Colab/.env sample
# Copy this file to `.env` (kept out of git) and replace the placeholder values
# with real credentials before running the notebook or FunctionTools.

# --- Required secrets (Notebook will fail without these) ---
ADK_PROJECT=sparkle-motion                       # ADK project slug used by Script/Production agents
ADK_API_KEY=sk-live-REPLACE_ME                   # Issue via ADK console or `adk keys create`

# --- Optional tokens (needed only for private Hugging Face repos) ---
HF_TOKEN=                                        # Leave blank unless HF_MODELS includes private repos

# --- Fixture toggle (leave at 1 unless you have a real ADK stack configured) ---
ADK_USE_FIXTURE=1                                # Keep at 1 on Colab; set to 0 only for real ADK runs

# --- Optional Workflow Agent overrides (defaults already match ToolRegistry: 5008/5001) ---
# Uncomment if you need to point notebooks at remote agents; otherwise leave unset.
# PRODUCTION_AGENT_BASE=http://127.0.0.1:5008
# SCRIPT_AGENT_BASE=http://127.0.0.1:5001

# --- Optional artifact storage overrides ---
ADK_ARTIFACTS_GCS_BUCKET=                        # Provide only if publishing artifacts to GCS
FINAL_VIDEO_DIR=/content/SparkleMotion/final_videos

# --- Optional smoke-test toggles (leave at 0 unless exercising real models) ---
SMOKE_IMAGES=0
IMAGES_SDXL_FIXTURE_ONLY=1
IMAGES_SDXL_MODEL=stabilityai/stable-diffusion-xl-base-1.0

SMOKE_VIDEOS=0
VIDEOS_WAN_FIXTURE_ONLY=1
VIDEOS_WAN_MODEL=Wan-AI/Wan2.1-FLF2V-14B-720P-diffusers

SMOKE_TTS=0
SMOKE_LIPSYNC=0
SMOKE_ASSEMBLE=0

# --- Optional notebook workspace helpers ---
WORKSPACE_NAME=SparkleMotion
MOUNT_POINT=/content/drive
